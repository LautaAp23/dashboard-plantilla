/**
 * @deprecated Importar desde "@/lib/auth-guard" en código nuevo.
 * Se mantiene por compatibilidad; re-exporta desde el guard centralizado.
 */
export { requireSession, requireSessionAdmin } from "@/lib/auth-guard"
